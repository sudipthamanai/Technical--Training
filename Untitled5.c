#include <stdio.h> 
#include <math.h> 
#include <stdlib.h>
int main()
{
int n,m,a,b,i,j;
printf("Enter the value of n:");
scanf("%d",&n);
for(i=0;i<n;i++)
{
scanf("%d",a[i]);	
}
int b[m];
printf("Enter the value of m:");
scanf("%d", &m);
for(j=0;j<m,j++)
{
scanf("%d",b[j]);	
}
for(i=0;i<n;i++)
{
	for(j=0;j<m,j++)
	{
		if(a[i]==b[j])
		{
			
		}
	}
}
printf("Enter the array elements:");

for(i=0;i<n;i++)
{
x=a[i];
while(x>0)
{
q=x%10;
sum=q;
if(q<s)
{
	s=q;
	z=a[i];
	
}
x=x/10;
}
}
printf("%d", a[i]);
return 0;
}
