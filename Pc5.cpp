/* An e-commerce website to find the lucky customer who will be eligible for full value cash back. For this purpose,  a number N is fed to the 
system. It will return another number that is calculated by an algorithm. in the algorithm, a sequence is generated,in which each number is the 
sum of the two preceeding numbers.initially the sequence will have two 1's in it. the system will return the Nth number from the generated sequence 
which is treated as the order ID. the lucky cusstomer will be the one who has placed the order. write an algorithm to help  the wesite find the lucky customer.*/

#include <stdio.h>
#include <string.h>
using namespace std;
int fabTerm(int n)
{
	int a=1,b=1,c,count=2;
	if()
}

