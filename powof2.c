#include <stdio.h>
#include <math.h>
int main()
{
    int n ,x , pow;
    printf("enter the number: ");
    scanf("%d",n);
    if (n == pow (2,x))
    {
        printf("true");
    }
    else
    {
        printf("false");
    }
    return 0;
} 
