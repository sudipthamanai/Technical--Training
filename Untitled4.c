#include <stdio.h>
#include <conio.h>
int main()
{
	char s[100];
	int i, len=0, l=0, u=0,d=0,sp=0,spl=0;
	scanf("[^\n]s", s);
	for(i=0;s[i]!='\0';l++)
	{
		len++;
	}
	for(i=0;i<len;i++)
	{
		if(isspace(s[i]))
}
