 /* you will be given  string of 'n' length then return the string without special charaters*/
 #include <iostream>
 #include <stdlib.h>
 #include <ctype.h>
using namespace std;
int* uniquearray(char *s,int len)
{
	char *y = (char*)malloc(len*sizeof(char));
	cin.getline(p)

}
int main()
{
	int i,n=0;
	char *p=(int*)malloc(1000*sizeof(char));
	cin>>p;
	for(i=0;p[i!='\0';i++)
	{
		n++;
	}
	p=(char*)realloc(p,n*sizeof(char));
	char *q=withoutSplchars(p,n);
	cout<<q;
	
}
